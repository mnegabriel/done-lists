{
  "name": "done-lists",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:clean": "vitest -u",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "jsdom": "^19.0.0",
    "vite": "^2.9.0",
    "vitest": "^0.9.3"
  }
}
